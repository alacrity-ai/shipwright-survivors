// src/game/boss/interfaces/BossPhaseMetadata.ts

export interface BossPhaseMetadata {
  // Add any relevant phase metadata here
  // For example:
  name: string;
  description: string;
  // ... other metadata
}
